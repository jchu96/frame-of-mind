# Web Test Agent Instructions

- Use `bun:test` and synthetic public-safe fixtures.
- Test storage parity, auth denial, contract mismatches, and migration drift.
- Never load a real run, transcript, recording, provider payload, or `.env`.
