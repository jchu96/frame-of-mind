# Web Script Agent Instructions

- Scripts are local operator tools and use Bun.
- Require explicit paths; do not scan home directories for runs.
- Print identifiers and paths only, never analysis bodies or credentials.
- Reuse the shared contract validator and RunStore.
